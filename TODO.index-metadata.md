you should use a lutaml::model::serializable to wrap the index collection for saving it to file, so that we have metadata (including the index schema version) in addition to the font index. FOr now, we know that the "last dated date of the index file" is the last time the index was updated. when the font index does not fit our latest schema, we need to rebuild the schema or migrate it (using the rebuild schema method that does a warm update).to