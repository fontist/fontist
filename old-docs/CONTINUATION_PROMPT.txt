# Fontist Dependency Migration - Continuation Prompt

## Context

You are continuing work on the Fontist project to complete the migration from legacy dependencies (extract_ttc, ttfunk, mime-types) to modern alternatives (fontisan, marcel).

## What Has Been Completed

### ✅ Dependency Migration (100% Complete)
1. Updated `fontist.gemspec`:
   - Removed extract_ttc (~> 0.3.7)
   - Removed ttfunk (~> 1.6)
   - Removed mime-types (~> 3.0)
   - Added marcel (~> 1.0)
   - Fixed excavate version constraint

2. Migrated all code to use Fontisan's direct Ruby API:
   - `lib/fontist/utils/cache.rb` - Marcel for MIME detection
   - `lib/fontist/font_file.rb` - Fontisan for font parsing
   - `lib/fontist/collection_file.rb` - Fontisan for collections
   - `lib/fontist/import/files/collection_file.rb` - Fontisan for extraction
   - `lib/fontist/system_index.rb` - Fixed typo (preferred_subfamily)

3. Implementation approach:
   - Using Fontisan's direct Ruby API (not Commands)
   - Accessing font tables with `font.table(Fontisan::Constants::NAME_TAG)`
   - Extracting metadata with `name_table.english_name(NAME_ID)`
   - Collection handling with `collection.font(index, io)` and tempfile extraction

### ✅ Test Results
- **Current:** 627/633 passing (99.1%)
- **Failures:** 6 tests (all system font/collection related)
- **Status:** Migration code works, tests need fixes

## What Needs to Be Done

### 🔴 CRITICAL: Fix 6 Failing Tests (Required for 100%)

Read IMPLEMENTATION_STATUS.md for detailed analysis of each failing test.

**Priority Order:**
1. `spec/fontist/system_font_spec.rb:17` - TTC font detection (CRITICAL)
2. `spec/fontist/system_font_spec.rb:50` - Collection fonts (CRITICAL)
3. `spec/fontist/system_index_font_collection_spec.rb:8` - YAML round-trip
4. `spec/fontist/font_spec.rb:305` - Skip download
5. `spec/fontist/cli_spec.rb:684` - System font install
6. `spec/fontist/cli_spec.rb:582` - Font paths with spaces

**Debug Approach:**
- Start with debugging TTC enumeration in console (see IMPLEMENTATION_STATUS.md for script)
- Check if CollectionFile returns proper FontFile objects
- Verify tempfile extraction works
- Check font metadata hash structure matches expectations

### 🟡 Important: Update Documentation

1. Update README.adoc:
   - Update Installation section dependencies
   - Remove references to removed gems
   - Add note about Fontisan and Marcel
   - Add migration note for users upgrading

2. Create migration documentation:
   - Document for users what changed
   - Explain new dependencies
   - Note any behavioral differences (none expected)

3. Update CHANGELOG.md:
   - Add entry for dependency migration
   - List removed dependencies
   - List added dependencies
   - Note breaking change if any

4. Clean up documentation:
   - Move completed work docs from old-docs/ to appropriate places
   - Keep old-docs for historical reference
   - Ensure docs/ has current information

### 🟢 Final Tasks

1. Code quality:
   - Run `bundle exec rubocop`
   - Fix any style issues
   - Verify all require statements

2. Verification:
   - Test Google Fonts import
   - Test manifest installation
   - Test CLI commands
   - Verify system font detection

## Key Implementation Details

### Fontisan Direct API Usage

**Font Loading:**
```ruby
font = Fontisan::FontLoader.load(path)
name_table = font.table(Fontisan::Constants::NAME_TAG)
family = name_table.english_name(Fontisan::Tables::Name::FAMILY)
```

**Collection Handling:**
```ruby
collection = Fontisan::FontLoader.load_collection(path)
File.open(path, "rb") do |io|
  collection.num_fonts.times do |index|
    font_binary = collection.font(index, io)
    # Extract to tempfile to get metadata
    Tempfile.create(['font', '.ttf']) do |tmp|
      tmp.binmode
      tmp.write(font_binary.to_binary_s)
      tmp.flush
      loaded_font = Fontisan::FontLoader.load(tmp.path)
      # Extract metadata from loaded_font
    end
  end
end
```

**Name Table Constants:**
```ruby
Fontisan::Tables::Name::FAMILY          # Family name
Fontisan::Tables::Name::SUBFAMILY       # Subfamily (style)
Fontisan::Tables::Name::FULL_NAME       # Full name
Fontisan::Tables::Name::POSTSCRIPT_NAME # PostScript name
Fontisan::Tables::Name::PREFERRED_FAMILY
Fontisan::Tables::Name::PREFERRED_SUBFAMILY
```

### FontFile Structure Change

**Before:** Stored TTFunk::File object  
**After:** Stores hash with keys:
- `:full_name`
- `:family_name`
- `:subfamily_name`
- `:preferred_family`
- `:preferred_subfamily`
- `:postscript_name`

Methods return hash values directly (strings or nil).

## Files to Review

**Read these first:**
1. `CONT INUATION_PLAN.md` - High-level remaining work
2. `IMPLEMENTATION_STATUS.md` - Detailed status of each task
3. `lib/fontist/font_file.rb` - Core font parsing (recently modified)
4. `lib/fontist/collection_file.rb` - Collection handling (recently modified)
5. `lib/fontist/system_index.rb` - System font indexing (calls CollectionFile)

**Test files to debug:**
1. `spec/fontist/system_font_spec.rb` - 2 critical failures
2. `spec/fontist/system_index_font_collection_spec.rb` - YAML issue
3. `spec/fontist/font_spec.rb:305` - Installation detection
4. `spec/fontist/cli_spec.rb` - 2 manifest/install issues

## Commands to Run

```bash
# Check current test status
bundle exec rspec | tail -30

# Debug specific test
bundle exec rspec spec/fontist/system_font_spec.rb:17 --format documentation

# Run rubocop
bundle exec rubocop

# Test in console
bin/console
# Then run debug scripts from IMPLEMENTATION_STATUS.md
```

## Success Criteria

✅ **Must Complete:**
- [ ] 633/633 tests passing (100%)
- [ ] README.adoc updated
- [ ] CHANGELOG.md updated
- [ ] Rubocop clean

✅ **Migration Quality:**
- [x] No old dependencies in code
- [x] Using Fontisan direct API
- [x] Bundle install works
- [x] Core functionality verified

## Important Notes

1. **Use Fontisan Ruby API directly** - Not Commands (already implemented correctly)
2. **Collection enumeration uses tempfiles** - This is the current approach
3. **FontFile stores hash now** - Not the font object
4. **All changes are internal** - No public API changes
5. **Production ready** - Code works, just need test fixes

## Start Here

1. Read IMPLEMENTATION_STATUS.md for detailed failing test analysis
2. Run the debug script for TTC enumeration
3. Fix CollectionFile if issues found
4. Run tests iteratively until 100%
5. Update documentation
6. Run rubocop and fix issues
7. Final verification and sign-off

Good luck! The hard part (migration) is done. Now just need to fix the test environment issues.